### Example environment variables for building the app with Docker / docker-compose
# Copy this file to .env and customize values before running docker-compose up --build

VITE_CMB_BASE=https://sso.example.com
VITE_JWT_EXPIRES=3600
PORT=5173
